import { DEFAULT_PAGING_OPTIONS } from "@/shared/const";
import { type PagingOptions } from "@/shared/types";

export const SPACE_PAGING_OPTIONS: PagingOptions = {
    ...DEFAULT_PAGING_OPTIONS,
    size: 18,
};
