import styled from "styled-components";

export const DraftSpaceListStyled = styled.section`
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
`;

export const ListStyled = styled.ul`
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
`;

export const TagInputStyled = styled.input`
    border: none;
    background: none;
    outline: none;
`;
